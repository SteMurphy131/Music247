<%= form_for(@user_label) do |f| %>
  <% if @user_label.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_label.errors.count, "error") %> prohibited this user_label from being saved:</h2>

      <ul>
      <% @user_label.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :label_id %><br>
    <%= f.number_field :label_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
